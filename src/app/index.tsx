import React from 'react';
import Layout from '../app/layout';

const Home: React.FC = () => {
  return (
    <Layout>
      <div>Welcome to the home page!</div>
    </Layout>
  );
};

export default Home;
